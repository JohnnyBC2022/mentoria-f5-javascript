let resultado;

//PI

resultado = Math.PI;

//Redondear
resultado = Math.round(2.8);
resultado = Math.round(2.2);
resultado = Math.round(2.6);
resultado = Math.round(2.5);

// Redondear hacia arriba
resultado = Math.ceil(2.1);

// Redondear hacia abajo
resultado = Math.floor(2.1);

// Raíz cuadrada
resultado = Math.sqrt(144);

// Potencia
resultado = Math.pow(2,3);

// Absoluto
resultado = Math.abs(-500);

// Mínimo
resultado = Math.min(3, 1, 12, -4,6 )

// Máximo
resultado = Math.max(3, 1, 12, -4,6 )

//Aleatorio
resultado = Math.random() // devuelve un valor aleatorio entre 0 y 1

//Aleatorio dentro de un rango
resultado = Math.floor(Math.random() * 255);




console.log(resultado);