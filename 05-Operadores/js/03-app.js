// Undefined
let numero;

console.log(numero);

/* Significa que una variable ha sido declarada pero no se le ha asignado ningún valor.
Es el valor por defecto de las variables no inicializadas. */


// Null

/* Es un valor asignado explícitamente a una variable para indicar que está vacía o no tiene ningún valor.
Representa la ausencia intencional de cualquier objeto o valor. */
let numero2 = null;
console(numero2);
console.log(typeof numero2);

console.log(numero == numero2);
console.log(numero === numero2);