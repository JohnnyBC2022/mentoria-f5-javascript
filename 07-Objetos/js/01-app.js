const nombre = "Monitor de 20 pulgadas";
const precio = 300;
const disponible = true;

/*  Object literal "Es una de las formas de construir objetos" ({propiedad: valor}) */

const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    disponible: true,
    saludar: function() {
        console.log(hola)
    }
}

console.table(producto)