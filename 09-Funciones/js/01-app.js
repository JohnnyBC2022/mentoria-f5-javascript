// Son un conjunto de procedimientos o instrucciones
// Son reutilizables.
// Código más limpio y ordenado.

// Declaración de funciones (Function Declaration)

function nombreFunción(parametrosFunción) {
    'cuerpo de la función (mis instrucciones)'
}

function sumar(numeros) {
    console.log(2 + 2)
}

// para que se ejecute la función hay que "llamarla"

sumar();
// por esto es reutilizable
sumar();
sumar();

// Expresión de función (Function Expression)

const sumar3 = function() {
    console.log(3 + 3)
}

sumar3();

